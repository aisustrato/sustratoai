// 📌 1. ESTANDARIZACIÓN DE TAMAÑOS
export type StandardBadgeSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg';
export type StandardBadgeStyleType = 'solid' | 'subtle' | 'outline';

// ⚠️ DEPRECATED: La lógica de generación y definiciones de tamaño se han movido a DesignTokensProvider.
// Mantenemos los tipos exportados para compatibilidad con consumidores existentes.
